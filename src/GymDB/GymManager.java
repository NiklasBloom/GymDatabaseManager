package GymDB;
import java.util.Scanner;

public class GymManager {
    public void run(){
        Scanner sc = new Scanner(System.in);
        String currentLine = sc.nextLine();
        while(!currentLine.equals("Q")){
            //do stuff

            //regex match one or two uppercase characters \b[A-Z]\b|\b[A-Z][A-Z]\b
        }
    }
}
